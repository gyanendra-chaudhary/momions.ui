import React from 'react';

const AppointmentHistory = () => {
    return (
        <div>
            {/* Your AppointmentHistory component content */}
        </div>
    );
};

export default AppointmentHistory;
